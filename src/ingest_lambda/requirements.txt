# Lambda Dependencies for Crypto Data Ingestion
# Install with: pip install -r requirements.txt -t .

# HTTP requests for API calls
requests>=2.31.0

# JSON handling (stdlib, but boto3 needs it)
urllib3>=2.0.0

# AWS SDK (included in Lambda runtime, but pin for local dev)
boto3>=1.34.0

# Data validation
pydantic>=2.5.0

# Date/time utilities
python-dateutil>=2.8.0
